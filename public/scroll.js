document.addEventListener("DOMContentLoaded", function () {
  const body = document.querySelector("body");

  body.style.scrollbarWidth = "thin"; // For Firefox
  body.style.scrollBehavior = "smooth"; // Smooth scrolling for navigation
});
